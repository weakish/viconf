# command #config_file location relative to $HOME #verify_syntax_command
# Seperated with ONE space character.
# Keep it sorted. ([0-9a-z])

awesome .config/awesome/rc.lua awesome --check --config
bash .bashrc bash -n
viconf .config/viconf/config viconf-check
